// Toast notifications are a planned future feature.
// When implemented, this module will expose a showToast() helper
// for programmatically creating ephemeral, stacked toast messages
// (e.g. "Settings saved", "Error connecting") — separate from the
// persistent full-width .banner system.
